@{
    ViewData["Title"] = "Statistic Evalution";
}
<style>
    /* Tinh chỉnh ComboBox */
    .form-select-lg {
        font-size: 14px; /* Giảm kích thước chữ */
        color: #555; /* Thêm màu tối nhẹ */
        padding: 5px 10px; /* Điều chỉnh padding */
        border: 1px solid #ced4da; /* Thêm viền */
        border-radius: 5px; /* Bo tròn các góc */
        transition: all 0.3s ease-in-out;
    }

    /* Hiệu ứng hover */
    .form-select-lg:hover {
        border-color: #495057;
        background-color: #f2f2f2;
    }
    #StatusStudentFilter {
        font-size: 14px;
        padding: 6px 12px;
        height: auto;
    }

    #exportFileStatusStudent {
        font-size: 14px;
        padding: 8px 16px;
    }


</style>
<div class="content-wrapper" style="min-height: 912.2px;">
    <!-- Header Phần Tiêu Đề -->
    <header class="content-header">
        <div class="container-fluid">
            <div class="row mb-2">
                <div class="col-sm-6">
                    <h3 class="m-0">Thống Kê Đánh Giá</h3>
                </div>
            </div>
        </div>
    </header>

    <!-- Nội Dung Chính -->
    <main class="content">
        <div class="container-fluid">
            <div class="card shadow-sm">
                <div class="card-body">
                    <!-- ComboBox thiết kế -->
                    <div class="form-group">
                        <label for="FromEvalutionFilter" style="font-size: 14px; color: #555;">Từ Niên Khóa:</label>
                        <select
                            id="FromEvalutionFilter"
                            class=" form-select-lg"
                            onchange="filterTable()"
                        >
                            <option selected="">Lựa Chọn Niên Khóa </option>
                            <option value="">2022-2025</option>
                           
                        </select>
                        <label for="ToEvalutionFilter" style="font-size: 14px; color: #555;">Đến Niên Khóa:</label>
                        <select
                            id="ToEvalutionFilter"
                            class=" form-select-lg"
                            onchange="filterTable()"
                        >
                            <option selected="">Lựa Chọn Niên Khóa </option>
                            <option value="">2024-2027</option>

                        </select>
                        <button
                            type="button"
                            class="btn btn-primary shadow-sm"
                            onclick="handleStatistics()"
                            style="font-size: 14px; margin-left: 15px;"
                        >
                            <i class="fas fa-chart-bar me-2"></i> Thống kê
                        </button>

                        <a type="button" class="btn btn-secondary" href="">
                            <i class="bi bi-download"></i> Xuất File
                        </a>
                    </div>

                    <!-- Bảng thông tin (trống) -->
                    <table class="table table-striped table-hover">
                        <thead class="text-center">
                        <tr>
                            <th scope="col">#</th>
                            <th scope="col">Mã Lớp</th>
                            <th scope="col">CVHT</th>
                            <th scope="col">Năm Học</th>
                            <th scope="col">Xếp Loại</th>
                        </tr>
                        </thead>
                        <tbody class="text-center">
                        </tbody>
                    </table>

                    <!-- Pagination -->
                    <nav aria-label="Page navigation">
                        <ul class="pagination justify-content-end">
                            <li class="page-item disabled">
                                <a class="page-link" aria-label="Previous">
                                    <span aria-hidden="true">&laquo;</span>
                                </a>
                            </li>
                            <li class="page-item active">
                                <a class="page-link">1</a>
                            </li>
                            <li class="page-item">
                                <a class="page-link" aria-label="Next">
                                    <span aria-hidden="true">&raquo;</span>
                                </a>
                            </li>
                        </ul>
                    </nav>
                </div>
            </div>
        </div>
    </main>
</div>
