@{
    ViewData["Title"] = "Show Statistic Status Student";
}
<style>
    /* Tinh chỉnh ComboBox */
    .form-select-lg {
        font-size: 14px; /* Giảm kích thước chữ */
        color: #555; /* Thêm màu tối nhẹ */
        padding: 5px 10px; /* Điều chỉnh padding */
        border: 1px solid #ced4da; /* Thêm viền */
        border-radius: 5px; /* Bo tròn các góc */
        transition: all 0.3s ease-in-out;
    }

    /* Hiệu ứng hover */
    .form-select-lg:hover {
        border-color: #495057;
        background-color: #f2f2f2;
    }
    #StatusStudentFilter {
        font-size: 14px;
        padding: 6px 12px;
        height: auto;
    }

    #exportFileStatusStudent {
        font-size: 14px;
        padding: 8px 16px;
    }


</style>
<div class="content-wrapper" style="min-height: 912.2px;">
    <!-- Header Phần Tiêu Đề -->
    <header class="content-header">
        <div class="container-fluid">
            <div class="row mb-2">
                <div class="col-sm-6">
                    <h3 class="m-0">Thống Kê Trạng Thái SV</h3>
                </div>
            </div>
        </div>
    </header>

    <!-- Nội Dung Chính -->
    <main class="content">
        <div class="container-fluid">
            <div class="card shadow-sm">
                <div class="card-body">
                    <!-- ComboBox thiết kế -->
                    <div class="form-group">
                        <label for="StatusStudentFilter" style="font-size: 14px; color: #555;"></label>
                        <select
                            id="StatusStudentFilter"
                            class=" form-select-lg"
                            onchange="filterTable()"
                        >
                            <option selected="">Lựa chọn Trạng Thái </option>
                            <option value="">Đang học</option>
                            <option value="">Bảo Lưu</option>
                            <option value="">Nghỉ Học</option>
                           
                        </select>
                        <button
                            id="exportFileStatusStudent"
                            class="btn btn-primary shadow-sm"
                            onclick="exportFile()"
                            style="font-size: 14px; margin-left: 15px"
                        >
                            <i class="fas fa-file-export me-2"></i> Xuất File
                        </button>
                    </div>

                    <!-- Bảng thông tin (trống) -->
                    <table class="table table-striped table-hover">
                        <thead class="text-center">
                        <tr>
                            <th scope="col">#</th>
                            <th scope="col">Mã SV</th>
                            <th scope="col">Eamil</th>
                            <th scope="col">Họ và Tên</th>
                            <th scope="col">Ngày tháng năm sinh	</th>
                            <th scope="col">Mã Lớp</th>
                            <th scope="col">Trạng Thái</th>
                        </tr>
                        </thead>
                        <tbody class="text-center">
                        </tbody>
                    </table>

                    <!-- Pagination -->
                    <nav aria-label="Page navigation">
                        <ul class="pagination justify-content-end">
                            <li class="page-item disabled">
                                <a class="page-link" aria-label="Previous">
                                    <span aria-hidden="true">&laquo;</span>
                                </a>
                            </li>
                            <li class="page-item active">
                                <a class="page-link">1</a>
                            </li>
                            <li class="page-item">
                                <a class="page-link" aria-label="Next">
                                    <span aria-hidden="true">&raquo;</span>
                                </a>
                            </li>
                        </ul>
                    </nav>
                </div>
            </div>
        </div>
    </main>
</div>
