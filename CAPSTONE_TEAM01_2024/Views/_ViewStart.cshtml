@* @{
    switch (ViewData["page"])
    {
        case "HomePage":
        case "SchoolYear":
        case "EndSemesterReport":
        case "SemesterPlan":
        case "Statistics":
        case "Index_ProfileManager":
        case "ShowStatistics":
        case "ClassList":
        case "StudentList":
        case "AdvisorProfiles":
        case "StudentProfiles":
        case "PersonalProfile":
            Layout = "_PersonnelLayout";
            break;
    }
} *@


@inject IHttpContextAccessor HttpContextAccessor

@{
    var rolesString = HttpContextAccessor.HttpContext.Session.GetString("Roles");
    var roles = rolesString?.Split(',') ?? new string[0];

    if (roles.Contains("Cố Vấn") || roles.Contains("BCN Khoa"))
    {
        Layout = "_PersonnelLayout";
    }
    else
    {
        Layout = "_StudentLayout";
    }
}